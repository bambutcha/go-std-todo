services:
  todo-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-server
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - PORT=${PORT:-8080}
    restart: unless-stopped

